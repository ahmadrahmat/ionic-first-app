import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.digitalks.vuememories',
  appName: 'Vue Memories',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
